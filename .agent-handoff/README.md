# Agent handoffs

This directory holds durable handoff metadata and local workflow queues.
Queue contents are intentionally ignored; the placeholder files preserve the
directory structure for fresh worktrees.
